#ifndef GENERATOR_ATTRIBUTES_H
#define GENERATOR_ATTRIBUTES_H

typedef enum {FILL,CHECKERED} Pattern;
typedef enum {L_SHAPE, U_SHAPE, T_SHAPE} Structure;
typedef enum {EASY, MEDIUM, HARD, INSANE} Mode;

#endif //GENERATOR_ATTRIBUTES_H